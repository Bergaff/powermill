# PDF парсинг
PyMuPDF>=1.24.0

# HTML парсинг (локальная справка PowerMill)
beautifulsoup4>=4.12.0

# Чанкинг
langchain>=0.2.16
langchain-community>=0.2.16
langchain-text-splitters>=0.2.4

# Векторная база
chromadb>=0.5.5

# Локальные эмбеддинги (~90 МБ, all-MiniLM-L6-v2)
sentence-transformers>=3.0.1

# LLM через Ollama
ollama>=0.3.3

# Видео → текст
faster-whisper>=1.0.3
yt-dlp>=2024.8.6

# Telegram-бот
aiogram>=3.12.0

# Утилиты
tqdm>=4.66.5
python-dotenv>=1.0.1
psutil>=6.0.0

# Внимание: torch (~2 ГБ) ставится вместе с sentence-transformers.
# Клонируй репозиторий на диск E — тогда venv и torch окажутся на E:,
# либо заранее выполни scripts\setup_paths.bat (PIP_CACHE_DIR тоже на E:).
