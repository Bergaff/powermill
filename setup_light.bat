@echo off
chcp 65001 >nul
title PowerMill AI - быстрая установка
cd /d "%~dp0"

echo =========================================================
echo   БЫСТРАЯ УСТАНОВКА (обязательные библиотеки, без тяжёлых)
echo =========================================================
echo.
echo Этот файл раньше создавал окружение в папке venv и ставил только
echo разбор справки. Теперь установка одна - install.bat:
echo   * окружение .venv (как ждёт вся программа);
echo   * обязательные библиотеки (pywin32, psutil, requests, bs4, lxml);
echo   * ярлыки, макросы для PowerMill, проверка компьютера;
echo   * тяжёлые библиотеки (ChromaDB, эмбеддинги) - по вопросу, можно позже.
echo.
echo Передаю в install.bat с ключом --light (без тяжёлых библиотек).
echo.

call "%~dp0install.bat" --light
exit /b %ERRORLEVEL%
